# Copy to .env and fill in real values.

DISCORD_TOKEN=
NARA_API_KEY=

# Your Discord user id — Mushy DMs this account for anything she needs (API keys,
# permissions, judgment calls) and it's the only account /set_server works for.
# Defaults to this if unset, but set it explicitly anyway.
OWNER_DISCORD_ID=951539463224451102

# Optional overrides
ROUTER_BASE_URL=https://router.bynara.id/v1
SONEM_CYCLE_MINUTES=20
SONEM_ROOT_DIR=
